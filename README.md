#Steps to execute this project: - 

1. Install docker on your local machine.
2. Use the command `git clone https://github.com/OmkarTerbhai/demo.git`
3. Navigate to `/demo`
4. Execute the command `docker build -t my-spring-app`
5. Execute the command `docker run -it --init my-spring-app`
